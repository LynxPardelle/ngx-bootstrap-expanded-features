import { BefInitDirective } from './bef-init.directive';

describe('BefInitDirective', () => {
  it('should create an instance', () => {
    const directive = new BefInitDirective();
    expect(directive).toBeTruthy();
  });
});
